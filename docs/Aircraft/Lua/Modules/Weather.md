# Weather Module

Weather and atmosphere helpers.

## Functions

---

### `getCloudsDensity` {: .unverified-section }

Unknown.

```lua
Weather.getCloudsDensity(...)
```

---

### `getGroundWindAtPoint` {: .unverified-section }

Unknown.

```lua
wind = Weather.getGroundWindAtPoint(pos)
```

---

### `getGroundWindComponents` {: .unverified-section }

Unknown.

```lua
Weather.getGroundWindComponents(...)
```

---

### `getGroundWindVelDir` {: .unverified-section }

Unknown.

```lua
Weather.getGroundWindVelDir(...)
```

---

### `getTemperatureAndPressureAtPoint` {: .unverified-section }

Returns temperature and pressure at a point.

```lua
temperature, pressure = Weather.getTemperatureAndPressureAtPoint(pos)
```

!!! note
    `pos` is expected to be shaped like `{ position = { x = 0, y = 0, z = 0 } }`.

---

### `getWindAtPoint` {: .unverified-section }

Unknown.

```lua
Weather.getWindAtPoint(...)
```

---

### `getWindComponents` {: .unverified-section }

Unknown.

```lua
Weather.getWindComponents(...)
```

---

### `getWindVelDir` {: .unverified-section }

Unknown.

```lua
Weather.getWindVelDir(...)
```

---

### `initAtmospere` {: .unverified-section }

Unknown.

```lua
Weather.initAtmospere(...)
```

---

### `updateSpeedDirForOtherLevel` {: .unverified-section }

Unknown.

```lua
Weather.updateSpeedDirForOtherLevel(...)
```
