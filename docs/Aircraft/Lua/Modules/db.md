# db Module

Database tables and helpers exposed by DCS.

## Fields

| Field | Type |
|-------|------|
| `Callnames` | `table` |
| `Countermeasures` | `table` |
| `Countries` | `table` |
| `CountriesByName` | `table` |
| `DefaultCountry` | `table` |
| `FormationID` | `table` |
| `Formations` | `table` |
| `Pods` | `table` |
| `Seasons` | `table` |
| `Sensors` | `table` |
| `Targets` | `table` |
| `Units` | `table` |
| `Weapons` | `table` |
| `callnamesRussia` | `table` |
| `localization` | `table` |
| `objectIconByType` | `table` |
| `rates` | `table` |
| `roles` | `table` |
| `typesNavPoints` | `table` |

## Functions

---

### `aircraftCallnames` {: .unverified-section }

Unknown.

```lua
db.aircraftCallnames(...)
```

---

### `declare_service_life_unit` {: .unverified-section }

Unknown.

```lua
db.declare_service_life_unit(...)
```

---

### `doesCallsignHasNumericPart` {: .unverified-section }

Unknown.

```lua
db.doesCallsignHasNumericPart(...)
```

---

### `getCallnames` {: .unverified-section }

Unknown.

```lua
db.getCallnames(...)
```

---

### `getCallnamesRefPoints` {: .unverified-section }

Unknown.

```lua
db.getCallnamesRefPoints(...)
```

---

### `getCountryOfOrigin` {: .unverified-section }

Unknown.

```lua
db.getCountryOfOrigin(...)
```

---

### `getHistoricalCountres` {: .unverified-section }

Unknown.

```lua
db.getHistoricalCountres(...)
```

---

### `getUnitCallnames` {: .unverified-section }

Unknown.

```lua
db.getUnitCallnames(...)
```

---

### `getUnitCallnames2` {: .unverified-section }

Unknown.

```lua
db.getUnitCallnames2(...)
```

---

### `getYearsLauncher` {: .unverified-section }

Unknown.

```lua
db.getYearsLauncher(...)
```

---

### `getYearsLocal` {: .unverified-section }

Unknown.

```lua
db.getYearsLocal(...)
```

---

### `isWesternCallnames` {: .unverified-section }

Unknown.

```lua
db.isWesternCallnames(...)
```
