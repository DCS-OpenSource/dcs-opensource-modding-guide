# sound Module

Sound and music helpers.

## Functions

---

### `exit` {: .unverified-section }

Unknown.

```lua
sound.exit()
```

---

### `getDevices` {: .unverified-section }

Unknown.

```lua
sound.getDevices()
```

---

### `getLayouts` {: .unverified-section }

Unknown.

```lua
sound.getLayouts()
```

---

### `getWaveInfo` {: .unverified-section }

Unknown.

```lua
sound.getWaveInfo(...)
```

---

### `init` {: .unverified-section }

Unknown.

```lua
sound.init(...)
```

---

### `pauseMusic` {: .unverified-section }

Pauses the current music.

```lua
sound.pauseMusic()
```

---

### `playMusic` {: .unverified-section }

Plays a playlist.

```lua
sound.playMusic(playlist)
```

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `playlist` | `table<string>` | Yes | Table of filenames. |

---

### `playPreview` {: .unverified-section }

Plays a preview of a sound file.

```lua
sound.playPreview(filename)
```

---

### `playSound` {: .unverified-section }

Plays an `ogg` or `wav` sound file.

```lua
sound.playSound(filename)
```

---

### `resumeMusic` {: .unverified-section }

Resumes the current music.

```lua
sound.resumeMusic()
```

---

### `setEffectsGain` {: .unverified-section }

Sets effects gain.

```lua
sound.setEffectsGain(gain)
```

---

### `setMusicGain` {: .unverified-section }

Sets music gain.

```lua
sound.setMusicGain(gain)
```

---

### `stopMusic` {: .unverified-section }

Stops the current music.

```lua
sound.stopMusic()
```

---

### `stopPreview` {: .unverified-section }

Stops the preview.

```lua
sound.stopPreview()
```

---

### `update` {: .unverified-section }

Unknown.

```lua
sound.update(...)
```

---

### `updateSettings` {: .unverified-section }

Unknown.

```lua
sound.updateSettings(...)
```
